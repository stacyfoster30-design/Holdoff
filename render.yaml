services:
  - type: web
    name: Holdoff-1
    runtime: node
    buildCommand: "echo 'HoldOff emergency static — no build required'"
    startCommand: "node emergency-static-server.js"
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
