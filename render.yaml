services:
  - type: web
    name: Holdoff-1
    runtime: node
    buildCommand: "echo 'No build required'"
    startCommand: "node emergency-static-server.js"
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20.11.1"
